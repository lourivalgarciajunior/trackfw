---
name: trackfw-governance
description: Inspect and maintain ADR, REQ, ROADMAP, traceability, lifecycle state and validation.
---

Run `trackfw context --format=json`, read the working context, preserve trace IDs and status-folder coherence, and run `trackfw validate --json` before reporting completion.
