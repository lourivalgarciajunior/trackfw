---
name: v2.4.1-baseline-ratchet-warnings-2026-06-13
title: "Roadmap v2.4.1 — Bugfix: ratchet de warnings no baseline + trim de aspas no parser YAML"
status: Done
req: docs/requisições/claude/REQ-2026-06-13-v2.4.1-baseline-ratchet-warnings.md
created: 2026-06-13
author: zeus
---

# Roadmap v2.4.1 — Bugfixes baseline e parser YAML

> Criado em: 2026-06-13 | Concluído em: 2026-06-13 | Status: ✅ Done
> REQ: [REQ-2026-06-13-v2.4.1-baseline-ratchet-warnings](../../../requisições/claude/REQ-2026-06-13-v2.4.1-baseline-ratchet-warnings.md)
> Branch: `fix/v2.4.1-baseline-ratchet-warnings`

**REGRA DURA:** Paridade nos 3 CLIs (Go + Node.js + Python) em todas as waves.

---

## Diagnóstico

| # | Bug | Impacto |
|---|-----|---------|
| B1 | Ratchet do baseline ignora `warnings` | Baseline inútil para passivo legado (maioria é warning) |
| B2 | Valores de regra entre aspas não reconhecidos | Usuário que copia do `trackfw help` quebra a config silenciosamente |

---

## Wave 1 — Fix B1: ratchet simétrico de warnings no baseline (3 MLs em paralelo)

> MLs independentes — cada CLI em arquivo distinto.

### ML-1A — Go
**Status:** ✅ Concluído

**Arquivo afetado:** `internal/validator/validator.go`

**Mudança em `Validate()`:**

Localizar o bloco do ratchet de violations (após `ValidateUnfiltered()`, antes do lenient mode).
Logo após filtrar violations, aplicar o mesmo set-difference em warnings:

```go
// Após: violations = netNew
warnSet := make(map[string]struct{}, len(baseline.Warnings))
for _, w := range baseline.Warnings {
    warnSet[w] = struct{}{}
}
var netNewWarn []string
for _, w := range warnings {
    if _, exists := warnSet[w]; !exists {
        netNewWarn = append(netNewWarn, w)
    }
}
warnings = netNewWarn
// Só então aplicar lenient mode
```

**Ordem obrigatória:** filtro de baseline (violations + warnings) → lenient mode. Nunca inverter.

**Testes a adicionar** em `internal/validator/validator_baseline_test.go`:
- `TestBaselineFiltersWarnings`: repo com warning (ex: ADR órfão com severidade "warning") → após baseline, warning não aparece em `Validate()`
- `TestBaselineLenientNoRecreate`: modo lenient + baseline → warning baselined não reaparece (lenient não recria)

**Critérios de aceite:**
- [x] `go test ./internal/validator/...` verde com 2 novos testes

---

### ML-1B — Node.js
**Status:** ✅ Concluído

**Arquivo afetado:** `npm/src/validator/index.js`

**Mudança em `validate()`:**

Localizar o bloco do ratchet (após `validateUnfiltered()`, antes do lenient mode).
Logo após filtrar violations, aplicar set-difference em warnings:

```js
// Após: violations = violations.filter(v => !baselineSet.has(v))
const baselineWarnSet = new Set(baseline.warnings || [])
warnings = warnings.filter(w => !baselineWarnSet.has(w))
// Só então aplicar lenient mode
```

**Testes a adicionar** em `npm/tests/baseline.test.js`:
- `baseline filtra warnings baselined`: warning presente no baseline não aparece após validate
- `lenient + baseline: warning baselined não reaparece`: modo lenient não recria warning já baselined

**Critérios de aceite:**
- [x] `node npm/tests/baseline.test.js` verde com 2 novos testes

---

### ML-1C — Python
**Status:** ✅ Concluído

**Arquivo afetado:** `pypi/trackfw/validator.py`

**Mudança em `validate()`:**

Localizar o bloco do ratchet (após `validate_unfiltered()`, antes do lenient mode).
Logo após filtrar violations, aplicar set-difference em warnings:

```python
# Após: violations = net_new
baseline_warn_set = set(baseline.get("warnings", []))
warnings = [w for w in warnings
            if _extract_messages([w])[0] not in baseline_warn_set]
# Só então aplicar lenient mode
```

**Testes a adicionar** em `pypi/tests/test_baseline.py`:
- `test_baseline_filters_warnings`: warning presente no baseline não aparece após validate
- `test_lenient_baseline_no_recreate`: lenient + baseline → warning baselined não reaparece

**Critérios de aceite:**
- [x] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q` verde com 2 novos testes

---

## Wave 2 — Fix B2: trim de aspas no parser YAML (3 MLs em paralelo)

> Dependência: Wave 1 completa (evitar conflitos em validator.go).

### ML-2A — Go
**Status:** ✅ Concluído

**Arquivo afetado:** `internal/config/config.go`

**Mudança:**

Na função `parse()`, localizar o trecho que processa sub-chaves indentadas do bloco `rules:`:

```go
// Trecho atual (aproximado):
k = strings.TrimSpace(...)
v = strings.TrimSpace(line[colon_idx+1:])
rules[k] = v
```

Adicionar trim de aspas no valor **antes** de armazenar:

```go
v = strings.Trim(v, "\"'")
rules[k] = v
```

Aplicar o mesmo trim a **todos** os valores escalares extraídos pelo parser (campos top-level e sub-chaves), para consistência e prevenção de regressão futura. Verificar se `extractFrontmatterField` já faz isso (sim, já faz) e tornar o parser de config igualmente robusto.

**Testes a adicionar** em `internal/config/config_evolution_test.go`:
- `TestRulesValueWithDoubleQuotes`: `rules:\n  adr_orphan: "off"` → `cfg.Rules["adr_orphan"] == "off"`
- `TestRulesValueWithSingleQuotes`: `rules:\n  stale_wip: 'warning'` → `cfg.Rules["stale_wip"] == "warning"`

**Critérios de aceite:**
- [x] `go build ./...` limpo
- [x] `go test ./internal/config/...` verde com 2 novos testes

---

### ML-2B — Node.js
**Status:** ✅ Concluído

**Arquivo afetado:** `npm/src/config/index.js`

**Mudança:**

Na função `_parse()`, localizar o trecho que processa sub-chaves indentadas do bloco `rules:`:

```js
// Trecho atual (aproximado):
const v = line.slice(colonIdx + 1).trim()
rules[k] = v
```

Adicionar trim de aspas:

```js
const v = line.slice(colonIdx + 1).trim().replace(/^["']|["']$/g, '')
rules[k] = v
```

Aplicar o mesmo replace a todos os valores escalares extraídos pelo parser para consistência.

**Testes a adicionar** em `npm/tests/config.test.js`:
- `rules com aspas duplas são reconhecidas`: `rules:\n  adr_orphan: "off"` → `cfg.rules.adr_orphan === "off"`
- `rules com aspas simples são reconhecidas`: `rules:\n  stale_wip: 'warning'` → `cfg.rules.stale_wip === "warning"`

**Critérios de aceite:**
- [x] `node npm/tests/config.test.js` verde com 2 novos testes

---

### ML-2C — Python
**Status:** ✅ Concluído

**Arquivo afetado:** `pypi/trackfw/config.py`

**Mudança:**

Na função `_parse()`, localizar o trecho que processa sub-chaves indentadas do bloco `rules:`:

```python
# Trecho atual (aproximado):
v = line[colon_idx + 1:].strip()
rules[k] = v
```

Adicionar trim de aspas:

```python
v = line[colon_idx + 1:].strip().strip("\"'")
rules[k] = v
```

Aplicar o mesmo strip a todos os valores escalares extraídos para consistência.

**Testes a adicionar** em `pypi/tests/test_config.py`:
- `test_rules_value_with_double_quotes`: `rules:\n  adr_orphan: "off"` → `cfg["rules"]["adr_orphan"] == "off"`
- `test_rules_value_with_single_quotes`: `rules:\n  stale_wip: 'warning'` → `cfg["rules"]["stale_wip"] == "warning"`

**Critérios de aceite:**
- [x] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q` verde com 2 novos testes

---

## Comandos de validação

```bash
# Wave 1 (ratchet de warnings)
go test ./internal/validator/...
node npm/tests/baseline.test.js
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_baseline.py -q

# Wave 2 (trim de aspas)
go build ./... && go test ./internal/config/...
node npm/tests/config.test.js
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_config.py -q

# Validação final
go build ./... && go test ./...
node npm/tests/baseline.test.js && node npm/tests/config.test.js && node npm/tests/validator.test.js
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q
```
