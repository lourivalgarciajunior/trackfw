---
name: v2.5-discovery-json-traceid
title: "v2.5 — Discovery Mode + validate --json + req_id bidirecional"
status: done
reqs:
  - docs/requisições/claude/REQ-2026-06-13-discovery-mode-cmdb.md
  - docs/requisições/claude/REQ-2026-06-13-validate-json-output.md
  - docs/requisições/claude/REQ-2026-06-13-traceid-bidirecional.md
created: 2026-06-13
author: zeus
branch: feat/v2.5-discovery-json-traceid
---

# Roadmap v2.5 — Discovery Mode + validate --json + req_id bidirecional

> Criado em: 2026-06-13 | Status: ✅ Done

## Diagnóstico / Contexto

Três evoluções identificadas após validação da v2.4.1 no projeto CMDB:

1. **validate --json** (quickest win): saída estruturada para CI/CD pipelines.
2. **Discovery Mode** (maior impacto de adoção): paths configuráveis + namespacing por agente +
   `trackfw discover` — sem isso, projetos com estrutura não-padrão (como o CMDB) não operam.
3. **req_id bidirecional** (rigor de rastreabilidade): verificação REQ↔Roadmap por ID estável.

Ordem de waves: --json é independente (Wave 1). Discovery Mode segue em 3 blocos sequenciais
(Waves 2-4). req_id depende de paths configuráveis do Bloco B (Wave 5).

Paridade obrigatória: Go · Node.js · Python nos 3 CLIs.

---

## Wave 1 — validate --json (3 MLs em paralelo)

> Dependências: independente — pode rodar em paralelo com qualquer outra wave

### ML-1A — validate --json (Go)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `internal/commands/validate.go`
- `internal/validator/validator.go` (ou struct de resultado)
- `internal/commands/validate_json_test.go` (novo)

**Ações:**
1. Adicionar flag `--json` ao `newValidateCmd()` em `validate.go`.
2. Criar struct `ValidateResult` em `validator.go` (ou arquivo novo):
   ```go
   type RuleItem struct {
       Rule    string `json:"rule"`
       File    string `json:"file"`
       Message string `json:"message"`
   }
   type ValidateResult struct {
       Summary    ValidateSummary `json:"summary"`
       Violations []RuleItem      `json:"violations"`
       Warnings   []RuleItem      `json:"warnings"`
   }
   type ValidateSummary struct {
       Violations int    `json:"violations"`
       Warnings   int    `json:"warnings"`
       Mode       string `json:"mode"`
       ExitCode   int    `json:"exit_code"`
   }
   ```
3. Quando `--json`: serializar resultado via `encoding/json` e imprimir em stdout; sair com mesmo exit code.
4. Sem `--json`: comportamento atual inalterado.
5. Para extrair `rule` por item: os messages atuais têm formato `"[RULE] file: msg"` ou similar — parsear ou refatorar `applyRule` para retornar struct em vez de string.

**Critérios de aceite:**
- [ ] `trackfw validate --json` emite JSON válido
- [ ] `summary.mode` = `"lenient"` ou `"strict"` conforme config
- [ ] Cada item tem `rule`, `file`, `message`
- [ ] Exit code idêntico ao sem `--json`
- [ ] `go test ./internal/commands/ -run TestValidateJSON` verde

**Comandos de validação:**
```bash
make build
go test ./internal/... -run TestValidateJSON -v
go test ./... 2>&1 | tail -5
```

---

### ML-1B — validate --json (Node.js)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `npm/src/commands/validate.js`
- `npm/src/validator/index.js` (adicionar `rule` nos itens)
- `npm/tests/validate_json.test.js` (novo)

**Ações:**
1. Adicionar opção `--json` ao comando validate em `validate.js`.
2. Em `index.js`, garantir que cada item de violations/warnings tenha `rule`, `file`, `message`.
   Atualmente as mensagens são strings — refatorar para objetos `{rule, file, message}`.
3. Quando `--json`: `JSON.stringify({summary, violations, warnings}, null, 2)` → stdout.
4. Sem `--json`: comportamento atual mantido (remontar string a partir dos objetos).

**Critérios de aceite:**
- [ ] `node npm/src/index.js validate --json` emite JSON válido
- [ ] Itens têm `rule`, `file`, `message`
- [ ] Saída texto sem `--json` inalterada
- [ ] `node npm/tests/validate_json.test.js` — todos passando

**Comandos de validação:**
```bash
node npm/tests/validate_json.test.js
node npm/tests/validate.test.js 2>/dev/null || true
```

---

### ML-1C — validate --json (Python)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `pypi/trackfw/commands/validate.py` (ou onde o validate está implementado)
- `pypi/trackfw/validator.py`
- `pypi/tests/test_validate_json.py` (novo)

**Ações:**
1. Adicionar argumento `--json` ao parser do comando validate.
2. Garantir que cada item de violations/warnings seja `{"rule": ..., "file": ..., "message": ...}`.
3. Quando `--json`: `import json; print(json.dumps(result, indent=2))`.
4. Sem `--json`: comportamento atual mantido.

**Critérios de aceite:**
- [ ] `trackfw validate --json` emite JSON válido
- [ ] Itens têm `rule`, `file`, `message`
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_validate_json.py -v` verde

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_validate_json.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Wave 2 — Discovery Mode Bloco B: Paths Configuráveis (3 MLs em paralelo)

> Dependências: Wave 1 concluída (ou independente — Wave 2 pode rodar em paralelo com Wave 1)

### ML-2A — Paths configuráveis (Go)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `internal/config/config.go`
- `internal/config/config_test.go`
- Todos os comandos que usam paths hardcoded (`validate.go`, `status.go`, `log.go`, etc.)

**Ações:**
1. Adicionar campos ao struct `Config` em `config.go`:
   ```go
   AdrDirs    []string `yaml:"adr_dirs"`    // default: ["docs/adr"]
   ReqDir     string   `yaml:"req_dir"`     // default: "docs/req"
   RoadmapDir string   `yaml:"roadmap_dir"` // default: "docs/roadmaps"
   ```
2. No parser, suportar lista em `adr_dirs` (múltiplos diretórios).
3. Fallback: se campo ausente → valor default (retrocompatível).
4. Todos os comandos que usam `docs/adr`, `docs/req`, `docs/roadmaps` hardcoded → ler de `cfg.AdrDirs`, `cfg.ReqDir`, `cfg.RoadmapDir`.
5. Detectar `docs/requisições/` (UTF-8) como `req_dir` válido.

**Critérios de aceite:**
- [ ] `adr_dirs: [docs/adr, docs/architecture]` → validate escaneia ambos
- [ ] `req_dir: "docs/requisições"` → validate encontra REQs com acento
- [ ] Ausência dos campos → defaults, comportamento atual inalterado
- [ ] `go test ./internal/config/ -v` verde

**Comandos de validação:**
```bash
make build
go test ./internal/config/ -v
go test ./internal/... -q 2>&1 | tail -5
```

---

### ML-2B — Paths configuráveis (Node.js)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `npm/src/config/index.js`
- `npm/src/validator/index.js`
- `npm/tests/config.test.js` (adicionar casos)

**Ações:**
1. Adicionar `adrDirs` (array, default `['docs/adr']`), `reqDir` (default `'docs/req'`),
   `roadmapDir` (default `'docs/roadmaps'`) ao parser de config.
2. Validator usa `cfg.adrDirs`, `cfg.reqDir`, `cfg.roadmapDir` em vez de paths hardcoded.
3. Suportar lista `adr_dirs` (bloco YAML `- item`).
4. Fallback para defaults quando campos ausentes.

**Critérios de aceite:**
- [ ] `adr_dirs` com múltiplos dirs → todos escaneados
- [ ] `req_dir: "docs/requisições"` → funciona
- [ ] Defaults retrocompatíveis
- [ ] `node npm/tests/config.test.js` verde

**Comandos de validação:**
```bash
node npm/tests/config.test.js
node npm/tests/validate.test.js 2>/dev/null || true
```

---

### ML-2C — Paths configuráveis (Python)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `pypi/trackfw/config.py`
- `pypi/trackfw/validator.py`
- `pypi/tests/test_config.py` (adicionar casos)

**Ações:**
1. Adicionar `adr_dirs` (list, default `["docs/adr"]`), `req_dir` (default `"docs/req"`),
   `roadmap_dir` (default `"docs/roadmaps"`) ao `_parse()`.
2. Suportar lista YAML em bloco (`- item`) para `adr_dirs`.
3. Validator usa os campos do config em vez de paths hardcoded.
4. Fallback para defaults.

**Critérios de aceite:**
- [ ] `adr_dirs` com múltiplos dirs → todos escaneados
- [ ] `req_dir: "docs/requisições"` → funciona (UTF-8)
- [ ] Defaults retrocompatíveis
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_config.py -v` verde

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_config.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Wave 3 — Discovery Mode Bloco C: Namespacing por Agente (3 MLs em paralelo)

> Dependências: Wave 2 concluída

### ML-3A — Namespacing por agente (Go)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `internal/config/config.go` (novos campos)
- `internal/validator/validator.go`
- `internal/commands/status.go`
- `internal/commands/roadmap.go`

**Ações:**
1. Adicionar ao `Config`:
   ```go
   RoadmapNamespacing string   `yaml:"roadmap_namespacing"` // "" | "by_agent"
   Agents             []string `yaml:"agents"`
   ```
2. Em modo `by_agent`:
   - Roadmap paths: `<roadmap_dir>/<agente>/<estado>/` (dois níveis)
   - `validate` verifica WIP limit por agente (não global)
   - `status` exibe breakdown por agente
   - `roadmap move`, `roadmap list`, `roadmap show` operam na hierarquia por agente
3. Sem `roadmap_namespacing` ou valor vazio → comportamento atual (um nível).

**Critérios de aceite:**
- [ ] `roadmap_namespacing: by_agent` + `agents: [zeus, apolo]` → validate opera em `docs/roadmaps/zeus/wip/` etc.
- [ ] WIP limit verificado por agente
- [ ] `status` exibe agentes separados
- [ ] Sem config → comportamento atual inalterado
- [ ] `go test ./internal/... -q` verde

**Comandos de validação:**
```bash
make build
go test ./internal/... -q 2>&1 | tail -5
```

---

### ML-3B — Namespacing por agente (Node.js)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `npm/src/config/index.js`
- `npm/src/validator/index.js`
- `npm/src/commands/status.js`

**Ações:**
1. Adicionar `roadmapNamespacing` e `agents` ao config.
2. Validator e status usam hierarquia `<roadmapDir>/<agente>/<estado>/` quando `by_agent`.
3. WIP limit verificado por agente.

**Critérios de aceite:**
- [ ] `roadmap_namespacing: by_agent` → hierarquia de dois níveis escaneada
- [ ] WIP limit por agente
- [ ] Retrocompatível sem config
- [ ] `node npm/tests/validate.test.js` verde (ou novos testes)

**Comandos de validação:**
```bash
node npm/tests/validate.test.js
node npm/tests/config.test.js
```

---

### ML-3C — Namespacing por agente (Python)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `pypi/trackfw/config.py`
- `pypi/trackfw/validator.py`
- `pypi/trackfw/commands/status.py` (se existir)

**Ações:**
1. Adicionar `roadmap_namespacing` e `agents` ao config.
2. Validator usa hierarquia de dois níveis quando `by_agent`.
3. WIP limit por agente.

**Critérios de aceite:**
- [ ] `roadmap_namespacing: by_agent` → hierarquia escaneada corretamente
- [ ] Retrocompatível
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q` verde

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Wave 4 — Discovery Mode Bloco A: trackfw discover (3 MLs em paralelo)

> Dependências: Waves 2 + 3 concluídas

### ML-4A — trackfw discover (Go)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `internal/commands/discover.go` (novo, substituir stub)
- `internal/generators/discover.go` (novo — lógica de descoberta)
- `internal/commands/discover_test.go` (novo)

**Ações:**
1. Implementar `trackfw discover` (sem flags): escaneia CWD e imprime relatório:
   - ADR dirs encontrados + contagem de ADRs
   - REQ dir + contagem
   - Roadmap dir + namespacing detectado + agentes (se `by_agent`)
   - Governance score 0-100 (heurística: % de artefatos com links válidos)
2. `trackfw discover --init`: gera `trackfw.yaml` calibrado para a estrutura + `governance_mode: lenient`.
3. `trackfw discover --bootstrap-log`: cria `.trackfw-log` retroativo a partir dos arquivos em `done/`.
4. Detectar `docs/requisições/` (UTF-8) como REQ dir.
5. Detectar `by_agent` automaticamente pela presença de `<roadmap_dir>/*/wip/`.

**Critérios de aceite:**
- [ ] `trackfw discover` imprime relatório legível com paths, contagens e score
- [ ] `trackfw discover --init` gera `trackfw.yaml` válido
- [ ] `trackfw discover --bootstrap-log` cria `.trackfw-log` com entradas de `done/`
- [ ] Detecta `docs/requisições/` como REQ dir
- [ ] Detecta modo `by_agent` automaticamente
- [ ] `go test ./internal/commands/ -run TestDiscover -v` verde

**Comandos de validação:**
```bash
make build
go test ./internal/commands/ -run TestDiscover -v
go test ./... -q 2>&1 | tail -5
```

---

### ML-4B — trackfw discover (Node.js)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `npm/src/commands/discover.js` (substituir stub)
- `npm/tests/discover.test.js` (novo)

**Ações:**
1. Implementar as mesmas 3 operações: `discover`, `--init`, `--bootstrap-log`.
2. Mesma lógica de detecção de estrutura e geração de `trackfw.yaml`.

**Critérios de aceite:**
- [ ] Mesmo comportamento que Go
- [ ] `node npm/tests/discover.test.js` verde

**Comandos de validação:**
```bash
node npm/tests/discover.test.js
```

---

### ML-4C — trackfw discover (Python)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `pypi/trackfw/commands/discover.py` (substituir stub se existir, ou criar)
- `pypi/trackfw/cli.py` (registrar comando se ainda stub)
- `pypi/tests/test_discover.py` (novo)

**Ações:**
1. Implementar as mesmas 3 operações: `discover`, `--init`, `--bootstrap-log`.
2. Registrar em `cli.py` substituindo o stub atual.

**Critérios de aceite:**
- [ ] Mesmo comportamento que Go
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_discover.py -v` verde

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_discover.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Wave 5 — req_id bidirecional (3 MLs em paralelo)

> Dependências: Wave 2 concluída (usa paths configuráveis para resolução de refs)

### ML-5A — req_id bidirecional (Go)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `internal/config/config.go` (campo `trace_id_field`)
- `internal/validator/validator.go` (nova regra)
- `internal/validator/validator_traceid_test.go` (novo)

**Ações:**
1. Adicionar `TraceIdField string` ao `Config` (default: `""` — desativado).
2. Em `Validate()`, quando `cfg.TraceIdField != ""`:
   - Indexar REQs por `req_id` (extrair do frontmatter pelo campo configurado)
   - Indexar Roadmaps por `req_id`
   - Verificar bidirecionalidade, compatibilidade de estado, unicidade
   - Emitir violations: `traceid_orphan_roadmap`, `traceid_orphan_req`,
     `traceid_state_mismatch`, `traceid_duplicate_req`, `traceid_duplicate_roadmap`
3. Sem `trace_id_field` → comportamento atual inalterado.

**Critérios de aceite:**
- [ ] Todos os 5 cenários de violation cobertos
- [ ] Par válido REQ+Roadmap com mesmo `req_id` → sem violation
- [ ] Sem `trace_id_field` → inalterado
- [ ] `go test ./internal/validator/ -run TestTraceId -v` verde

**Comandos de validação:**
```bash
make build
go test ./internal/validator/ -run TestTraceId -v
go test ./... -q 2>&1 | tail -5
```

---

### ML-5B — req_id bidirecional (Node.js)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `npm/src/config/index.js` (`traceIdField`)
- `npm/src/validator/index.js` (nova lógica)
- `npm/tests/traceid.test.js` (novo)

**Ações:**
1. Adicionar `traceIdField` ao config (default: `null`).
2. Implementar mesma lógica bidirecional no validator quando `traceIdField` definido.
3. Emitir mesmos 5 tipos de violation.

**Critérios de aceite:**
- [ ] Mesmos 5 cenários cobertos
- [ ] `node npm/tests/traceid.test.js` — todos passando

**Comandos de validação:**
```bash
node npm/tests/traceid.test.js
node npm/tests/validate.test.js 2>/dev/null || true
```

---

### ML-5C — req_id bidirecional (Python)

**Status:** ⬜ Pendente  
**Arquivos afetados:**
- `pypi/trackfw/config.py` (`trace_id_field`)
- `pypi/trackfw/validator.py` (nova lógica)
- `pypi/tests/test_traceid.py` (novo)

**Ações:**
1. Adicionar `trace_id_field` ao config (default: `""`).
2. Implementar mesma lógica bidirecional.
3. Emitir mesmos 5 tipos de violation.

**Critérios de aceite:**
- [ ] Mesmos 5 cenários cobertos
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_traceid.py -v` verde

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_traceid.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -q 2>&1 | tail -5
```

---

## Resumo de dependências

```
Wave 1 (--json)    ──────────────────────────────────────────► independente
Wave 2 (paths)     ──────────────────────────────────────────► independente
Wave 3 (by_agent)  ← Wave 2
Wave 4 (discover)  ← Waves 2 + 3
Wave 5 (traceid)   ← Wave 2
```

Waves 1 e 2 podem correr em paralelo. Wave 3 e Wave 5 podem correr em paralelo após Wave 2.
Wave 4 só começa após Waves 2 e 3.

## Legenda
- ⬜ Pendente
- 🔄 Em andamento
- ✅ Concluído
- ❌ Bloqueado
