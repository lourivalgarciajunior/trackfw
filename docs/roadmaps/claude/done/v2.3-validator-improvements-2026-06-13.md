---
name: v2.3-validator-improvements-2026-06-13
title: "Roadmap v2.3 — Melhorias no validador (adr_dirs recursivo, stale git log, existência de refs, pasta×status, unicidade)"
status: Done
req: docs/requisições/claude/REQ-2026-06-13-validator-improvements.md
created: 2026-06-13
author: zeus
---

# Roadmap v2.3 — Melhorias no validador

> Criado em: 2026-06-13 | Status: ✅ Done
> REQ: [REQ-2026-06-13-validator-improvements](../../../requisições/claude/REQ-2026-06-13-validator-improvements.md)
> Branch: `feat/v2.3-validator-improvements` (criada a partir de `feat/v2.2-python-cli-nativo`)

**REGRA DURA:** Toda mudança deve ser implementada nos 3 CLIs (Go + Node.js + Python). Nenhum ML conclui sem paridade nos 3.

**Origem:** análise comparativa do agente CMDB em `docs/analise-cmdb/analise-comparativa-gates-governanca.md`.

---

## Diagnóstico

5 problemas identificados no `trackfw validate` atual:

| # | Tipo | Problema |
|---|------|---------|
| B1 | Bug | `adr_dirs` não-recursivo → checks de ADR viram no-ops silenciosos com subpastas |
| B2 | Bug | Stale WIP por `mtime` → instável em CI (clone zera o mtime) |
| M3 | Melhoria | Referências a arquivos inexistentes (`REQ:`, `ADR:`, `Roadmap:`) não são verificadas |
| M4 | Melhoria | Sem coerência pasta × `status:` declarado no frontmatter |
| M5 | Melhoria | Mesmo filename em dois estados passa sem detecção |

---

## Wave 1 — Implementação nos 3 CLIs (MLs em paralelo)

> MLs independentes — arquivos distintos, nenhuma dependência entre si.

### ML-1A — Go (`internal/validator/validator.go`)
**Status:** ✅ Concluído

**Arquivos afetados:**
- `internal/validator/validator.go`
- `internal/validator/validator_improvements_test.go` (novo)

**Critérios de aceite:**
- [x] `go build ./...` sem erros
- [x] `go test ./internal/validator/...` verde (24/24 testes)
- [x] ADRs em subpastas são detectados pelos checks #2, #5, #9, #10
- [x] `validateStaleWIP` usa `git log` com fallback para `mtime`
- [x] Refs inexistentes geram warnings
- [x] Divergência pasta×status gera warning
- [x] Filename duplicado gera violation

---

### ML-1B — Node.js (`npm/src/validator/index.js`)
**Status:** ✅ Concluído

**Arquivos afetados:**
- `npm/src/validator/index.js`
- `npm/tests/validator.test.js` (novo)

**Critérios de aceite:**
- [x] `node -e "require('./npm/src/validator').validate().then(r => console.log(r))"` sem erro
- [x] 12/12 testes passando (`node npm/tests/validator.test.js`)

---

### ML-1C — Python (`pypi/trackfw/validator.py`)
**Status:** ✅ Concluído

**Arquivos afetados:**
- `pypi/trackfw/validator.py`
- `pypi/tests/test_validator.py`

**Critérios de aceite:**
- [x] `PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"` verde
- [x] 157/157 testes passando (146 pré-existentes + 11 novos)

---

## Resultado final

| CLI | Testes | Status |
|-----|--------|--------|
| Go | 24/24 | ✅ |
| Node.js | 12/12 | ✅ |
| Python | 157/157 | ✅ |

Todos os 5 problemas corrigidos/implementados com paridade nos 3 CLIs.
