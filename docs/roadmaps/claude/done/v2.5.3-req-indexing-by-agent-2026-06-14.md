---
name: v2.5.3-req-indexing-by-agent
title: "v2.5.3 — Fix: coleta de REQs não honra roadmap_namespacing: by_agent"
status: done
req: docs/requisições/claude/REQ-2026-06-14-req-indexing-by-agent.md
created: 2026-06-14
author: zeus
branch: fix/v2.5.3-req-indexing-by-agent
---

# Roadmap v2.5.3 — Fix REQ indexing em by_agent

> Criado em: 2026-06-14 | Status: ✅ Done

## Diagnóstico

A v2.5.2 corrigiu o scanner de Roadmaps para `by_agent`, mas o scanner de REQs permanece plano.
Em projetos com `req_dir/<agente>/<estado>/` (ADR-036), todas as checagens de REQ ficam inertes.
No CMDB real: REQs (0), Roadmaps (116) → 15 falsos `traceid_orphan_roadmap`.

**Funções afetadas por CLI:**

| CLI | Funções | Problema |
|-----|---------|---------|
| Go | `validateREQsHaveADR`, `validateREQsHaveRoadmap`, `blockedREQs`, `validateREQsNotBlockedByDraftADRs`, `validateFrontmatterPresence`, `validateRefTargetsExist`, `validateTraceId` | `listDir(cfg.REQDir)` / `filepath.Glob(cfg.REQDir + "/*.md")` — flat |
| Node.js | mesmas 6+ funções | `listDir(cfg.reqDir)` — flat |
| Python | mesmas 6+ funções | `list_dir(cfg["req_dir"])` — flat |

---

## Wave 1 — Fix REQ scanner by_agent + salvaguarda (3 MLs em paralelo)

> Dependências: independentes entre si; requerem branch `fix/v2.5.3-req-indexing-by-agent`

### ML-1A — Go

**Status:** ✅ Concluído

**Arquivos afetados:**
- `internal/validator/validator.go`
- `internal/validator/validator_traceid.go`
- `internal/validator/validator_test.go`

**Ações:**

1. Em `validator.go`, adicionar helper `resolveREQFiles(cfg config.ProjectConfig) []string`:
   - Se `cfg.RoadmapNamespacing == config.NamespacingByAgent`:
     - Descobrir agentes via `cfg.Agents` ou `os.ReadDir(cfg.REQDir)` (igual ao `collectTraceIdEntriesByAgent`)
     - Para cada agente, para cada estado em `["backlog", "wip", "blocked", "done", "abandoned"]`:
       - `filepath.Glob(filepath.Join(cfg.REQDir, agent, state, "*.md"))`
   - Senão: `filepath.Glob(filepath.Join(cfg.REQDir, "*.md"))`

2. Substituir todas as chamadas planas de REQ pelo helper:
   - `validateREQsHaveADR` (linha ~754): `listDir(cfg.REQDir)` → `resolveREQFiles(cfg)`
   - `validateREQsHaveRoadmap` (linha ~794): `listDir(cfg.REQDir)` → `resolveREQFiles(cfg)`
   - `blockedREQs` (linha ~908): `listDir(cfg.REQDir)` → `resolveREQFiles(cfg)`
   - `validateREQsNotBlockedByDraftADRs` (linha ~945): `filepath.Glob(cfg.REQDir + "/*.md")` → `resolveREQFiles(cfg)`
   - `validateFrontmatterPresence` (linha ~1073): `filepath.Glob(cfg.REQDir + "/*.md")` → `resolveREQFiles(cfg)`
   - `validateRefTargetsExist` (linha ~1199): `listDir(cfg.REQDir)` → `resolveREQFiles(cfg)`

3. Em `validator_traceid.go`, função `validateTraceId`:
   - Substituir `reqEntries, _ = collectTraceIdEntries(cfg.REQDir, traceField)` por:
     - Se `cfg.RoadmapNamespacing == config.NamespacingByAgent`:
       `reqEntries, _ = collectTraceIdEntriesByAgent(cfg.REQDir, traceField, cfg)`
     - Senão: comportamento atual

4. Melhorar salvaguarda em `validator_traceid.go`:
   - Além do check `len(reqEntries) == 0 && len(roadmapEntries) == 0`,
     adicionar: se apenas um lado for 0, emitir warning específico:
     `"trace_id_field is set but REQs (0) were indexed while Roadmaps (N) were — check req_dir and roadmap_namespacing"`
     (e o inverso quando Roadmaps são 0 mas REQs não)

5. Adicionar testes em `validator_test.go`:
   - `TestREQIndexingByAgent`: REQ em `req_dir/claude/wip/`, `validateREQsHaveRoadmap` deve encontrá-la
   - `TestTraceIdREQByAgent`: REQ + Roadmap by_agent pareados → sem `traceid_orphan_roadmap`
   - `TestSalvaguardaOneSided`: Roadmaps indexados, REQs 0 → warning one-sided

**Critérios de aceite:**
- [ ] `go build ./...` limpo
- [ ] `go test ./internal/validator/...` verde (incluindo novos testes)
- [ ] `resolveREQFiles` retorna paths corretos em modo by_agent e flat

**Comandos de validação:**
```bash
cd /Users/kgsaran/Sistemas/Desenvolvimento/workspace/trackfw
go build ./...
go test ./internal/validator/... -v -run TestREQIndexingByAgent
go test ./internal/validator/... -v -run TestTraceIdREQByAgent
go test ./internal/validator/... -v -run TestSalvaguardaOneSided
go test ./internal/validator/... -count=1
```

---

### ML-1B — Node.js

**Status:** ✅ Concluído

**Arquivos afetados:**
- `npm/src/validator/index.js`
- `npm/src/validator/traceid.js`
- `npm/tests/req_by_agent.test.js` (novo)

**Ações:**

1. Em `npm/src/validator/index.js`, adicionar helper `resolveReqFiles(cfg)`:
   - A função `listDir` (linha 13) é flat. O helper substitui as chamadas a `listDir(cfg.reqDir)` quando by_agent:
   - Se `cfg.roadmapNamespacing === 'by_agent'` (ou `cfg.roadmap_namespacing`):
     - Descobrir agentes: `cfg.agents || fs.readdirSync(cfg.reqDir).filter(isDir)`
     - Para cada agente, para cada estado em `['backlog', 'wip', 'blocked', 'done', 'abandoned']`:
       - `fs.readdirSync(path.join(cfg.reqDir, agent, state)).filter(.md)` → append full paths
   - Senão: `listDir(cfg.reqDir)` atual
   - Retornar array de paths completos (não basenames)

2. Verificar que as funções de validator que atualmente recebem basenames de `listDir`
   e constroem path com `path.join(cfg.reqDir, name)` — o novo helper retorna paths completos,
   então as funções devem usar o path diretamente (não recompor com `cfg.reqDir`).
   Ajustar as chamadas de uso conforme necessário.

3. Substituir em todas as funções (linhas ~175, 211, 234, 427, 452, 535, 583, 725 de `index.js`):
   `listDir(cfg.reqDir)` → chamar `resolveReqFiles(cfg)` e ajustar uso de `name` para path completo

4. Em `npm/src/validator/traceid.js`, função `checkTraceIds`:
   - A indexação de REQs usa `walkMd(reqDir)` que já é recursivo — **REQs já são encontradas**.
   - Verificar se há limitação de estado inferido de REQ em by_agent (o `stateFromPath` vai pegar o estado da subpasta de estado).
   - Melhorar salvaguarda: além de `reqIndex.size === 0 && roadmapIndex.size === 0`,
     adicionar: se apenas um lado for 0, retornar warning one-sided.

5. Criar `npm/tests/req_by_agent.test.js` com testes:
   - `resolveReqFiles` flat: retorna arquivos de `req_dir/`
   - `resolveReqFiles` by_agent: retorna arquivos de `req_dir/claude/wip/`
   - Validator `validateREQsHaveRoadmap` encontra REQs by_agent
   - Salvaguarda one-sided: Roadmaps > 0, REQs = 0 → warning

**Critérios de aceite:**
- [ ] `node npm/tests/req_by_agent.test.js` verde (todos os novos testes PASS)
- [ ] Testes existentes sem regressão: `node npm/tests/traceid_by_agent.test.js`

**Comandos de validação:**
```bash
cd /Users/kgsaran/Sistemas/Desenvolvimento/workspace/trackfw
node npm/tests/req_by_agent.test.js
node npm/tests/traceid_by_agent.test.js
node npm/tests/validate_json.test.js
```

---

### ML-1C — Python

**Status:** ✅ Concluído

**Arquivos afetados:**
- `pypi/trackfw/validator.py`
- `pypi/trackfw/traceid.py`
- `pypi/tests/test_req_by_agent.py` (novo)

**Ações:**

1. Em `pypi/trackfw/validator.py`, adicionar `resolve_req_files(cfg: dict) -> list[str]`:
   - Se `cfg.get("roadmap_namespacing") == "by_agent"`:
     - `agents = cfg.get("agents", [])` ou `os.listdir(req_dir)` filtrando dirs
     - Para cada agente, para cada estado em `["backlog", "wip", "blocked", "done", "abandoned"]`:
       - `glob(os.path.join(req_dir, agent, state, "*.md"))`
   - Senão: `glob(os.path.join(req_dir, "*.md"))` flat
   - Retornar lista de paths completos

2. Substituir chamadas flat nas funções (linhas ~430, 467, 489, 642, 687, 725):
   - `list_dir(cfg["req_dir"])` → `resolve_req_files(cfg)`
   - As funções que iteravam sobre `name` e compunham `os.path.join(req_dir, name)` devem
     usar o path completo retornado pelo helper diretamente.

3. Em `pypi/trackfw/traceid.py`, função `check_traceid`:
   - Substituir: `req_entries = _index_reqs(req_dir, field)` por:
     - Se `namespacing == "by_agent"`:
       `req_entries = _index_reqs_by_agent(req_dir, field, cfg.get("agents", []))`
     - Senão: `_index_reqs(req_dir, field)` (comportamento atual)
   - Adicionar `_index_reqs_by_agent(req_dir, field, agents)` espelhando `_index_roadmaps_by_agent`
     mas com estado derivado da pasta (os estados canônicos se aplicam ao req_dir também em by_agent)

4. Melhorar salvaguarda em `check_traceid`:
   - Além de `not req_entries and not roadmap_entries`, adicionar:
     - Se `req_entries` vazio mas `roadmap_entries` não: warning one-sided sobre REQs
     - Se `roadmap_entries` vazio mas `req_entries` não: warning one-sided sobre Roadmaps

5. Criar `pypi/tests/test_req_by_agent.py` com testes:
   - `test_resolve_req_files_flat`: flat retorna `.md` de `req_dir/`
   - `test_resolve_req_files_by_agent`: by_agent retorna `.md` de `req_dir/claude/wip/`
   - `test_traceid_req_by_agent`: REQ + Roadmap by_agent pareados → sem `traceid_orphan_roadmap`
   - `test_salvaguarda_one_sided`: Roadmaps > 0, REQs = 0 → warning one-sided

**Critérios de aceite:**
- [ ] `/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_req_by_agent.py -v` verde
- [ ] Testes existentes sem regressão

**Comandos de validação:**
```bash
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_req_by_agent.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/test_traceid.py -v
/private/tmp/trackfw-test-venv/bin/python -m pytest pypi/tests/ -v
```

---

## Critérios de aceite globais

- [x] Branch `fix/v2.5.3-req-indexing-by-agent` criada
- [x] Go: `resolveREQFiles` by_agent + REQ scan traceid by_agent + salvaguarda one-sided + testes (25/25)
- [x] Node.js: `resolveReqFiles` by_agent + salvaguarda one-sided + testes (111/111)
- [x] Python: `resolve_req_files` by_agent + `_index_reqs_by_agent` + salvaguarda one-sided + testes (236/236)
- [x] Paridade confirmada nos 3 CLIs
- [x] `go build ./...` limpo
- [x] Todos os testes verdes nos 3 CLIs

## Legenda
- ⬜ Pendente
- 🔄 Em andamento
- ✅ Concluído
- ❌ Bloqueado
