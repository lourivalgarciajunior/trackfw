---
name: v2.4-config-evolution-2026-06-13
title: "Roadmap v2.4 — Config evolution: field mapping, baseline/ratchet, severidade por regra, wizard e help"
status: Done
req: docs/requisições/claude/REQ-2026-06-13-v2.4-config-evolution.md
created: 2026-06-13
author: zeus
---

# Roadmap v2.4 — Config evolution

> Criado em: 2026-06-13 | Concluído em: 2026-06-13 | Status: ✅ Done
> REQ: [REQ-2026-06-13-v2.4-config-evolution](../../../requisições/claude/REQ-2026-06-13-v2.4-config-evolution.md)
> Branch: `feat/v2.4-config-evolution`

**REGRA DURA:** Paridade nos 3 CLIs (Go + Node.js + Python) em todas as waves. Exceção explícita: UX do wizard (F6) pode variar, mas o YAML gerado deve ser idêntico.

**Dependência de waves:** Wave 1 → Wave 2 → Wave 3 → Wave 4. Dentro de cada wave, os 3 MLs são paralelos.

---

## Diagnóstico

| # | Problema | Impacto |
|---|----------|---------|
| P1 | Marcadores de link hardcoded (`REQ:`, `ADR:`) | Falsos positivos em projetos com convenção própria |
| P2 | `lenient` global esconde todo o passivo sem distinção | Adoção zero ou gate inútil |
| P3 | Rigor liga/desliga global | Times em maturação não conseguem adoção progressiva |
| P4 | Configuração opaca, sem help integrado | Onboarding difícil, wizard ausente |

---

## Wave 1 — Config extenso: parser de seções aninhadas + novos campos (MLs em paralelo)

> MLs independentes — arquivos distintos, sem dependência entre si.
> Pré-requisito das waves seguintes — sem esta wave, F2/F3/F4 não têm onde ler sua config.

### ML-1A — Go (`internal/config/config.go`)
**Status:** ✅ Concluído — commit `c676d45` · 12/12 testes verdes

**Arquivos afetados:**
- `internal/config/config.go`

**Mudanças:**

Adicionar novos campos à struct `ProjectConfig`:
```go
// Novos campos
LinkFieldsReq      []string // default: ["REQ:"]
LinkFieldsADR      []string // default: ["ADR:"]
LinkFieldsRoadmap  []string // default: ["Roadmap:"]
AcceptanceMarkers  []string // default: ["## Acceptance Criteria", "## Critérios de Aceite"]
Rules              map[string]string // default: ver defaults()
```

Atualizar `defaults()`:
```go
func defaults() ProjectConfig {
    return ProjectConfig{
        ADRDirs:            []string{"docs/adr"},
        REQDir:             "docs/req",
        RoadmapDir:         "docs/roadmaps",
        RoadmapNamespacing: "flat",
        WipLimit:           1,
        LinkFieldsReq:      []string{"REQ:"},
        LinkFieldsADR:      []string{"ADR:"},
        LinkFieldsRoadmap:  []string{"Roadmap:"},
        AcceptanceMarkers:  []string{"## Acceptance Criteria", "## Critérios de Aceite"},
        Rules: map[string]string{
            "wip_has_req":          "error",
            "wip_acceptance":       "error",
            "wip_limit":            "error",
            "stale_wip":            "warning",
            "adr_orphan":           "warning",
            "ref_targets_exist":    "warning",
            "folder_status":        "warning",
            "filename_uniqueness":  "error",
            "blocked_by_draft_adr": "error",
        },
    }
}
```

Estender `parse()` para suportar blocos aninhados de 1 nível. Adicionar estados:
- `inLinkFieldsReq`, `inLinkFieldsADR`, `inLinkFieldsRoadmap` (listas após `  req:`, `  adr:`, `  roadmap:`)
- `inAcceptanceMarkers` (lista após `acceptance_markers:`)
- `inRules` (pares `  key: value` após `rules:`)
- `inLinkFields` (bloco após `link_fields:`)

Lógica do parser estendido (pseudocódigo):
```
ao encontrar "link_fields:" → inLinkFields = true
ao encontrar "  req:" dentro de inLinkFields → inLinkFieldsReq = true
ao encontrar "  - valor" dentro de inLinkFieldsReq → append(linkFieldsReq, valor)
ao encontrar "  adr:" dentro de inLinkFields → flush req, inLinkFieldsADR = true
ao encontrar "  roadmap:" dentro de inLinkFields → flush adr, inLinkFieldsRoadmap = true
ao encontrar "acceptance_markers:" → inAcceptanceMarkers = true
ao encontrar "  - valor" dentro de inAcceptanceMarkers → append(acceptanceMarkers, valor)
ao encontrar "rules:" → inRules = true
ao encontrar "  key: value" dentro de inRules → rules[key] = value
qualquer linha sem prefixo de espaço → flush estado atual e sair do bloco aninhado
```

**Testes a adicionar em `internal/config/`:**
- `TestConfigDefaults`: sem `trackfw.yaml` → todos os defaults esperados
- `TestConfigLinkFields`: yaml com `link_fields.req: [req_id, "REQ:"]` → `LinkFieldsReq = ["req_id", "REQ:"]`
- `TestConfigAcceptanceMarkers`: yaml com lista customizada → aplicado
- `TestConfigRules`: yaml com `rules:\n  stale_wip: error` → `Rules["stale_wip"] == "error"`, outros mantêm default
- `TestConfigSparse`: yaml somente com `wip_limit: 3` → restante usa defaults

**Critérios de aceite:**
- [ ] `go build ./...` sem erros
- [ ] `go test ./internal/config/...` verde
- [ ] `ProjectConfig` expõe `LinkFieldsReq`, `LinkFieldsADR`, `LinkFieldsRoadmap`, `AcceptanceMarkers`, `Rules`
- [ ] Retrocompatível: `trackfw.yaml` existente sem novos campos → comportamento idêntico à v2.3

---

### ML-1B — Node.js (`npm/src/config/index.js`)
**Status:** ✅ Concluído — commit `84eeff0` · 6/6 testes verdes

**Arquivos afetados:**
- `npm/src/config/index.js`

**Mudanças:**

Atualizar `defaults()`:
```js
function defaults() {
  return {
    adrDirs: ['docs/adr'],
    reqDir: 'docs/req',
    roadmapDir: 'docs/roadmaps',
    roadmapNamespacing: 'flat',
    agents: [],
    governanceMode: '',
    lenientUntil: '',
    wipLimit: 1,
    wipBySquad: false,
    requireReqInCommit: false,
    linkFields: {
      req: ['REQ:'],
      adr: ['ADR:'],
      roadmap: ['Roadmap:'],
    },
    acceptanceMarkers: ['## Acceptance Criteria', '## Critérios de Aceite'],
    rules: {
      wip_has_req:          'error',
      wip_acceptance:       'error',
      wip_limit:            'error',
      stale_wip:            'warning',
      adr_orphan:           'warning',
      ref_targets_exist:    'warning',
      folder_status:        'warning',
      filename_uniqueness:  'error',
      blocked_by_draft_adr: 'error',
    },
  }
}
```

Estender `parse()` com os mesmos estados do ML-1A (lógica idêntica em JS):
- Detectar `link_fields:` → entrar em bloco, coletar sub-chaves `req:`, `adr:`, `roadmap:` e suas listas
- Detectar `acceptance_markers:` → coletar lista
- Detectar `rules:` → coletar pares `key: value` com indentação

**Testes a adicionar em `npm/tests/`:**
- Arquivo `config.test.js` (novo) com os mesmos 5 casos do ML-1A

**Critérios de aceite:**
- [ ] `node -e "const c = require('./npm/src/config'); c.reset(); console.log(JSON.stringify(c.load()))"` exibe todos os defaults
- [ ] `npm/tests/config.test.js` verde

---

### ML-1C — Python (`pypi/trackfw/config.py`)
**Status:** ✅ Concluído — commit `201e748` · 11/11 testes verdes (163 total)

**Arquivos afetados:**
- `pypi/trackfw/config.py`
- `pypi/tests/test_config.py`

**Mudanças:**

Atualizar `defaults()`:
```python
def defaults():
    return {
        "adr_dirs": ["docs/adr"],
        "req_dir": "docs/req",
        "roadmap_dir": "docs/roadmaps",
        "roadmap_namespacing": "flat",
        "agents": [],
        "governance_mode": "",
        "lenient_until": "",
        "wip_limit": 1,
        "wip_by_squad": False,
        "require_req_in_commit": False,
        "link_fields": {
            "req": ["REQ:"],
            "adr": ["ADR:"],
            "roadmap": ["Roadmap:"],
        },
        "acceptance_markers": ["## Acceptance Criteria", "## Critérios de Aceite"],
        "rules": {
            "wip_has_req":          "error",
            "wip_acceptance":       "error",
            "wip_limit":            "error",
            "stale_wip":            "warning",
            "adr_orphan":           "warning",
            "ref_targets_exist":    "warning",
            "folder_status":        "warning",
            "filename_uniqueness":  "error",
            "blocked_by_draft_adr": "error",
        },
    }
```

Estender `_parse()` com a mesma lógica de blocos aninhados dos MLs anteriores.

Adicionar testes equivalentes em `pypi/tests/test_config.py` (classe `TestConfigEvolution`).

**Critérios de aceite:**
- [ ] `PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_config*"` verde
- [ ] Retrocompatível: `test_config.py` existente continua verde

---

## Wave 2 — Validators consomem novos campos de config (MLs em paralelo)

> Pré-requisito: Wave 1 completa (config expõe LinkFieldsReq, AcceptanceMarkers, Rules).

### ML-2A — Go (validator)
**Status:** ✅ Concluído — commit `0b0e47a` · 4 novos testes verdes

**Arquivos afetados:**
- `internal/validator/validator.go`

**Mudança F2 — Field mapping:**

Em `validateWIPHasREQ(content, path string) bool`:
- Substituir `strings.Contains(content, "REQ:")` por:
```go
cfg := config.Load()
for _, marker := range cfg.LinkFieldsReq {
    if strings.Contains(content, marker) { return true }
}
return false
```
Mesma lógica para ADR em `validateADRsAreReferenced` e Roadmap em `validateRefTargetsExist`.

Em `validateWIPAcceptanceCriteria(content string) bool`:
- Substituir os marcadores hardcoded por loop sobre `cfg.AcceptanceMarkers`.

**Mudança F3 — Severidade por regra:**

Adicionar helper:
```go
func ruleSeverity(name string) string {
    cfg := config.Load()
    if s, ok := cfg.Rules[name]; ok { return s }
    return "error" // fallback seguro
}
```

Adicionar helper para emitir resultado conforme severidade:
```go
func applyRule(name, message string, violations, warnings *[]string) {
    switch ruleSeverity(name) {
    case "off":     // silencioso
    case "warning": *warnings = append(*warnings, message)
    default:        *violations = append(*violations, message)
    }
}
```

Substituir todos os `violations = append(violations, msg)` e `warnings = append(warnings, msg)` por `applyRule("nome_da_regra", msg, &violations, &warnings)` nos seguintes pontos:
- `wip_has_req` → `validateWIPHasREQ`
- `wip_acceptance` → `validateWIPHasAcceptanceCriteria`
- `wip_limit` → `validateWIPLimit`
- `stale_wip` → `validateStaleWIP`
- `adr_orphan` → `validateADRsAreReferenced`
- `ref_targets_exist` → `validateRefTargetsExist`
- `folder_status` → `validateFolderStatusCoherence`
- `filename_uniqueness` → `validateFilenameUniqueness`
- `blocked_by_draft_adr` → `validateREQsNotBlockedByDraftADRs`

**Testes a adicionar:**
- `TestFieldMappingReqId`: yaml com `link_fields.req: [req_id]`, roadmap com `req_id: REQ-001` → sem violation
- `TestRuleSeverityOff`: regra `adr_orphan: off` → ADR órfão não aparece em violations nem warnings
- `TestRuleSeverityWarning`: regra `wip_has_req: warning` → "sem REQ" aparece em warnings, não violations
- `TestAcceptanceMarkersCustom`: yaml com marcador customizado → usado na validação

**Critérios de aceite:**
- [ ] `go build ./...` sem erros
- [ ] `go test ./internal/validator/...` verde (todos os testes anteriores + novos)
- [ ] Regra com `off` → não aparece no output
- [ ] `link_fields` customizado → sem falsos positivos de convenção

---

### ML-2B — Node.js (validator)
**Status:** ✅ Concluído — commit `6ed3ed5` · 16/16 testes verdes

**Arquivos afetados:**
- `npm/src/validator/index.js`

Mesmas mudanças do ML-2A, em JavaScript:
- `validateWIPHasREQ`: loop sobre `cfg.linkFields.req`
- `validateWIPHasAcceptanceCriteria`: loop sobre `cfg.acceptanceMarkers`
- Helper `ruleSeverity(name)` e `applyRule(name, msg, violations, warnings)`
- Substituir todos os appends por `applyRule`

**Critérios de aceite:**
- [ ] `node npm/tests/validator.test.js` verde
- [ ] Novos testes equivalentes ao ML-2A passando

---

### ML-2C — Python (validator)
**Status:** ✅ Concluído — commit `86c133a` · 167/167 testes verdes

**Arquivos afetados:**
- `pypi/trackfw/validator.py`
- `pypi/tests/test_validator.py`

Mesmas mudanças do ML-2A, em Python:
- `_validate_wip_has_req`: loop sobre `cfg["link_fields"]["req"]`
- `_validate_wip_acceptance`: loop sobre `cfg["acceptance_markers"]`
- Helper `_rule_severity(name, cfg)` e `_apply_rule(name, msg, violations, warnings, cfg)`
- Substituir todos os appends por `_apply_rule`

**Critérios de aceite:**
- [ ] `PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"` verde (157+ testes)

---

## Wave 3 — Baseline snapshot + catraca (MLs em paralelo)

> Independente das waves anteriores — pode ser implementada em paralelo com Wave 2 se desejado.

### ML-3A — Go (`trackfw baseline`)
**Status:** ✅ Concluído — commit `88456fd` · 34/34 testes verdes

**Arquivos afetados:**
- `internal/commands/baseline.go` (novo)
- `internal/validator/validator.go` (leitura de baseline em `Validate()`)

**Comando `trackfw baseline`:**
```go
// Executa Validate() e grava resultado em .trackfw-baseline.json
type BaselineFile struct {
    Created    string   `json:"created"`
    Violations []string `json:"violations"`
    Warnings   []string `json:"warnings"`
}
```
- Rodar `validator.Validate()`
- Serializar `BaselineFile` com `json.Marshal` e gravar em `.trackfw-baseline.json`
- Imprimir: `"Baseline gravado: N violations, M warnings"`

**Leitura de baseline em `Validate()`:**
- Se `.trackfw-baseline.json` existir no CWD, ler e deserializar
- Após calcular violations, filtrar: `netNew = violations - baseline.Violations` (subtração de conjunto por igualdade de string)
- Retornar `netNew` como violations (warnings novos exibidos mas não bloqueiam)
- Adicionar campo `BaselineActive bool` em `ValidationResult` para o output indicar modo baseline

**Testes:**
- `TestBaselineCreation`: rodar baseline, verificar `.trackfw-baseline.json` criado com violations corretas
- `TestBaselineFiltersOldViolations`: com baseline existente, violations do baseline não aparecem no resultado
- `TestBaselineNetNewViolation`: com baseline existente, violation nova (não no baseline) → bloqueia

**Critérios de aceite:**
- [x] `trackfw baseline` grava `.trackfw-baseline.json`
- [x] `trackfw validate` com baseline presente → só falha em violations novas
- [x] `go test ./internal/...` verde

---

### ML-3B — Node.js (`trackfw baseline`)
**Status:** ✅ Concluído — commit `77b8f8a` · 4/4 baseline + 16/16 validator

**Arquivos afetados:**
- `npm/src/commands/baseline.js` (novo)
- `npm/src/validator/index.js` (leitura de baseline em `validate()`)

Mesma lógica do ML-3A em JavaScript.
- Usar `JSON.stringify` / `JSON.parse` para serializar
- `fs.writeFileSync('.trackfw-baseline.json', ...)`

**Critérios de aceite:**
- [x] `node -e "require('./npm/src/commands/baseline').run()"` cria `.trackfw-baseline.json`
- [x] Testes equivalentes ao ML-3A passando

---

### ML-3C — Python (`trackfw baseline`)
**Status:** ✅ Concluído — commit `e139d1a` · 171/171 testes verdes

**Arquivos afetados:**
- `pypi/trackfw/commands/baseline.py` (novo)
- `pypi/trackfw/validator.py` (leitura de baseline em `validate()`)
- `pypi/trackfw/cli.py` (registrar subcomando `baseline`)

Mesma lógica do ML-3A em Python.
- Usar `json.dumps` / `json.loads`
- `open('.trackfw-baseline.json', 'w').write(...)`

**Critérios de aceite:**
- [x] `PYTHONPATH=pypi python3 -m trackfw baseline` cria `.trackfw-baseline.json`
- [x] `PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"` verde

---

## Wave 4 — `trackfw help` + `trackfw configure` (wizard) (MLs em paralelo)

> Pré-requisito: Waves 1–3 completas (wizard precisa conhecer todos os campos).

### ML-4A — Go
**Status:** ✅ Concluído — commit `1b226bc` · 3/3 testes verdes

**Arquivos afetados:**
- `internal/commands/help.go` (novo) — `trackfw help [key]`
- `internal/commands/configure.go` (novo) — `trackfw configure` (wizard com `huh`)

**`trackfw help [key]`:**

Definir mapa interno `configDocs`:
```go
var configDocs = map[string]ConfigKeyDoc{
    "adr_dirs": {
        Type:        "list of strings",
        Default:     `["docs/adr"]`,
        Description: "Diretórios onde os ADRs são armazenados. Suporta múltiplos caminhos.",
        Example:     "adr_dirs:\n  - docs/adr\n  - docs/architecture/decisions",
        Impact:      "Afeta todas as verificações de ADR (órfão, draft, referência).",
    },
    "wip_limit": { ... },
    "link_fields.req": { ... },
    "rules.stale_wip": { ... },
    // ... todos os campos
}
```
- Sem argumento: lista todas as keys em formato tabular (key | default | descrição curta)
- Com argumento: exibe doc completa da key

**`trackfw configure` (wizard):**

Usar `huh` para perguntas interativas. Fluxo:
1. Detectar `trackfw.yaml` existente → oferecer atualizar ou recriar
2. Perguntar: `adr_dirs` (default: `docs/adr`)
3. Perguntar: `req_dir`, `roadmap_dir`, `wip_limit`
4. Perguntar: convenção de link REQ (`REQ:` inline ou campo de frontmatter)
5. Perguntar: severidade de regras (exibir lista, permitir ajustar individualmente)
6. Gerar `trackfw.yaml` com **somente** os campos que diferem dos defaults

**Critérios de aceite:**
- [x] `trackfw help` lista todas as keys
- [x] `trackfw help wip_limit` exibe doc completa
- [x] `trackfw configure` gera `trackfw.yaml` mínimo (sem campos no valor default)
- [x] `go build ./...` + `go test ./...` verdes

---

### ML-4B — Node.js
**Status:** ✅ Concluído — commit `7f8ebda` · 11/11 testes verdes

**Arquivos afetados:**
- `npm/src/commands/help.js` (novo)
- `npm/src/commands/configure.js` (novo)

**`trackfw help [key]`:** mesma lógica do ML-4A, usando `console.log`.

**`trackfw configure`:** wizard interativo com `readline` (built-in Node.js, sem dep externa).
- Mesma sequência de perguntas do ML-4A
- Gera `trackfw.yaml` mínimo

**Critérios de aceite:**
- [x] `node -e "require('./npm/src/commands/help').run()"` lista keys
- [x] `node -e "require('./npm/src/commands/help').run('wip_limit')"` exibe doc
- [x] Testes equivalentes ao ML-4A para `help`

---

### ML-4C — Python
**Status:** ✅ Concluído — commit `5906adf` · 183/183 testes verdes

**Arquivos afetados:**
- `pypi/trackfw/commands/help_cmd.py` (novo, evitar conflito com builtin `help`)
- `pypi/trackfw/commands/configure.py` (novo)
- `pypi/trackfw/cli.py` (registrar `help` e `configure`)

**`trackfw help [key]`:** mesma lógica, usando `print()`.

**`trackfw configure`:** wizard interativo com `input()` (stdlib Python, sem dep externa).
- Exibe cada pergunta com valor default entre colchetes: `ADR dirs [docs/adr]: `
- Enter vazio usa o default
- Gera `trackfw.yaml` mínimo

**Critérios de aceite:**
- [x] `PYTHONPATH=pypi python3 -m trackfw help` lista keys
- [x] `PYTHONPATH=pypi python3 -m trackfw help wip_limit` exibe doc
- [x] `PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"` verde

---

## Comandos de validação por wave

```bash
# Wave 1 (config)
go build ./... && go test ./internal/config/...
node npm/tests/config.test.js
PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_config*"

# Wave 2 (validator)
go test ./internal/validator/...
node npm/tests/validator.test.js
PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_validator*"

# Wave 3 (baseline)
go test ./internal/...
node npm/tests/baseline.test.js
PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"

# Wave 4 (help + configure)
go build ./... && go test ./...

# Validação final (3 CLIs completos)
go build ./... && go test ./... && PYTHONPATH=pypi python3 -m unittest discover -s pypi/tests -p "test_*.py"
```
