---
status: done
date: 2026-06-14
req: "docs/requisições/claude/REQ-2026-06-14-trackfw-serve-ui.md"
squad: "zeus + apolo + afrodite"
---

# Roadmap: v2.7.0 — trackfw serve Dashboard Web (paridade Go · Node.js · Python)

> Criado em: 2026-06-14 | Status: 🔄 WIP

## Diagnóstico / Contexto

O `trackfw serve` hoje é um stub HTTP sem UI (Go apenas). A proposta é evoluir
para um dashboard web completo com paridade nos 3 CLIs — Go, Node.js e Python.

Os dados já existem: artefatos MD em disco, grafo de dependências via frontmatter,
log de transições em `.trackfw-log`. Node.js e Python já têm `parseLog`/`_parse_log`
e `calculate`/`_calculate` prontos em `commands/metrics.js` e `commands/metrics.py`
— reutilizar, não reescrever.

Assets web (HTML/JS/CSS) são **compartilhados** entre os 3 CLIs:
- Go: `embed.FS` (single-binary)
- Node.js: `npm/src/serve/static/` instalado com o pacote
- Python: `pypi/trackfw/serve/static/` via `package_data`

---

## Wave 0 — Assets compartilhados (HTML/JS/CSS) — base para os 3 CLIs
> Dependências: nenhuma | ✅ ML-1A Go já concluído (serve.go base)

### ML-0A — Dashboard HTML/JS/CSS completo (assets estáticos)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/static/index.html` (substituir placeholder do ML-1A)
- `internal/serve/static/app.js` (novo)
- `internal/serve/static/style.css` (novo)

**Ações:**
1. `index.html` com estrutura completa:
   - TailwindCSS via CDN
   - HTMX via CDN
   - marked.js via CDN (client-side markdown rendering)
   - Chart.js via CDN (burndown e donut)
   - D3.js via CDN (chain view force-directed graph)
   - Estrutura: nav lateral (Board / Chain / Metrics) + área principal + drawer lateral
2. `app.js` com:
   - `loadBoard()` → fetch `/api/board` → renderizar colunas kanban
   - `openDrawer(path)` → fetch `/api/file?path=` → `marked.parse()` → renderizar drawer
   - Interceptar links internos no markdown renderizado (`a[href]` relativos) → abrir no drawer
   - `loadChain()` → fetch `/api/chain` → D3.js force-directed graph
   - Click em nó do grafo → `openDrawer(path)`
   - `loadMetrics()` → fetch `/api/metrics` → KPI cards + Chart.js donut + line chart burndown
   - Filtro por agente no kanban (modo by_agent)
3. `style.css` — estilos mínimos complementares ao Tailwind (drawer animation, etc.)

**Critérios de aceite:**
- [ ] Kanban renderiza colunas com cards
- [ ] Cards clicáveis abrem drawer com markdown renderizado (frontmatter como tabela)
- [ ] Links no markdown abrem artefato referenciado no drawer
- [ ] Chain View renderiza grafo com nós coloridos por tipo/estado
- [ ] Métricas: KPI cards + donut chart + burndown line chart
- [ ] Filtro por agente funcional

---

## Wave 1 — Backend Go: endpoints API
> Dependências: nenhuma (paralela com Wave 0) | ML-1A ✅ concluído

### ML-1B — `GET /api/board` (Go)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/api_board.go` (novo)

**Ações:**
1. Handler `boardHandler`:
   - `config.Load()` para ler `RoadmapDir` e `RoadmapNamespacing`
   - Scan de roadmaps por estado (flat e by_agent)
   - Extrair título do `# ` heading ou do filename
   - JSON: `{ "columns": { "wip": [...], "backlog": [...] }, "agents": ["zeus"] }`
   - Card: `{ "file": "ROADMAP-...", "title": "...", "state": "wip", "agent": "zeus", "path": "docs/roadmaps/..." }`
2. Registrar em `serve.go`

**Critérios de aceite:**
- [ ] `curl localhost:8080/api/board` retorna JSON válido
- [ ] Modo by_agent: campo `agent` preenchido
- [ ] `go build ./...` && `go test ./...` verde

### ML-1C — `GET /api/chain` (Go)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/api_chain.go` (novo)

**Ações:**
1. Handler `chainHandler`:
   - Scan de ADRs, REQs e ROADMAPs
   - Extrair links do frontmatter: campos `adr:`, `req:`, `roadmap:`
   - Nós: `{ "id": "path/...", "type": "adr|req|roadmap", "title": "...", "state": "..." }`
   - Arestas: `{ "from": "...", "to": "..." }`
2. Registrar em `serve.go`

**Critérios de aceite:**
- [ ] `curl localhost:8080/api/chain` retorna `{ "nodes": [...], "edges": [...] }`
- [ ] Nós incluem ADRs, REQs e ROADMAPs com type e state corretos

### ML-1D — `GET /api/metrics` (Go)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/api_metrics.go` (novo)
- `internal/serve/metrics_log.go` (novo — parser do `.trackfw-log`)

**Ações:**
1. Parser `parseLog(path)` → `[]Transition{Timestamp, Basename, From, To}`
2. Calcular: `lead_time_avg_days`, `cycle_time_avg_days`, `abandonment_rate`,
   `state_distribution`, `burndown []{ date, open, closed }`
3. Retornar JSON com todos os campos; zeros se log ausente

**Critérios de aceite:**
- [ ] `curl localhost:8080/api/metrics` retorna JSON com todos os campos
- [ ] Log ausente → métricas zeradas sem erro
- [ ] Burndown tem pontos semanais com dados reais

### ML-1E — `GET /api/file` (Go)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/api_file.go` (novo)

**Ações:**
1. Handler `fileHandler(cfg)`:
   - Query param `path`
   - `filepath.Clean` + allowlist prefix check (ADRDir, REQDir, RoadmapDir)
   - Path fora dos dirs → 403
   - Path traversal → 403
   - Retorna conteúdo raw `text/plain; charset=utf-8`
2. Registrar em `serve.go`

**Critérios de aceite:**
- [ ] Conteúdo MD retornado corretamente
- [ ] `../../../etc/passwd` → 403
- [ ] Path fora dos dirs configurados → 403

---

## Wave 2 — Backend Node.js: `trackfw serve`
> Dependências: Wave 0 (assets prontos para copiar)

### ML-2A — `npm/src/commands/serve.js` + servidor HTTP Node.js
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `npm/src/commands/serve.js` (novo)
- `npm/src/serve/` (novo diretório)
- `npm/src/serve/api_board.js` (novo)
- `npm/src/serve/api_chain.js` (novo)
- `npm/src/serve/api_metrics.js` (novo — reutiliza `parseLog` de `commands/metrics.js`)
- `npm/src/serve/api_file.js` (novo)
- `npm/src/serve/static/` (copiar assets de `internal/serve/static/`)
- `npm/src/index.js` (adicionar comando serve)
- `npm/package.json` (adicionar `"files"` entry para `src/serve/static/`)

**Ações:**
1. Servidor com módulo `http` nativo (sem Express):
   - Roteamento manual por `req.url`
   - Assets estáticos lidos de `path.join(__dirname, 'static')`
   - Mesmos 4 endpoints da spec (`/api/board`, `/api/chain`, `/api/metrics`, `/api/file`)
2. `api_metrics.js`: importar e reutilizar `parseLog` de `../commands/metrics.js`
3. Flag `--port` (default 8080), `--open` (default true em não-CI)
4. `serve.js` exporta `createServeCommand()` e registra no commander

**Critérios de aceite:**
- [ ] `node npm/src/index.js serve --no-open` sobe sem erro
- [ ] `curl localhost:8080/api/board` retorna JSON
- [ ] `curl localhost:8080/api/metrics` retorna JSON (reutilizando parseLog)
- [ ] Path traversal em `/api/file` → 403
- [ ] Assets estáticos servidos corretamente

---

## Wave 3 — Backend Python: `trackfw serve`
> Dependências: Wave 0 (assets prontos para copiar)

### ML-3A — `pypi/trackfw/commands/serve.py` + servidor HTTP Python
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `pypi/trackfw/commands/serve.py` (novo)
- `pypi/trackfw/serve/` (novo diretório)
- `pypi/trackfw/serve/__init__.py` (novo)
- `pypi/trackfw/serve/api_board.py` (novo)
- `pypi/trackfw/serve/api_chain.py` (novo)
- `pypi/trackfw/serve/api_metrics.py` (novo — reutiliza `_parse_log` de `commands/metrics.py`)
- `pypi/trackfw/serve/api_file.py` (novo)
- `pypi/trackfw/serve/static/` (copiar assets de `internal/serve/static/`)
- `pypi/trackfw/cli.py` (adicionar subcomando serve)
- `pypi/setup.cfg` ou `pyproject.toml` (adicionar `package_data` para `serve/static/`)

**Ações:**
1. Servidor com `http.server.BaseHTTPRequestHandler` (stdlib, zero deps):
   - Roteamento por `self.path`
   - Assets estáticos lidos via `importlib.resources` ou `__file__`
   - Mesmos 4 endpoints
2. `api_metrics.py`: importar e reutilizar `_parse_log` de `commands.metrics`
3. Flag `--port` (default 8080), `--open` (default true fora de CI)
4. `serve.py` exporta `cmd_serve(args)` registrado no `cli.py`

**Critérios de aceite:**
- [ ] `python -m trackfw serve --no-open` sobe sem erro
- [ ] `curl localhost:8080/api/board` retorna JSON
- [ ] `curl localhost:8080/api/metrics` retorna JSON (reutilizando _parse_log)
- [ ] Path traversal em `/api/file` → 403 (HTTP 403 com body de erro)
- [ ] Assets estáticos instalados com o pacote (verificar com `pip install -e .`)

---

## Wave 4 — Qualidade + integração final (3 CLIs)
> Dependências: Waves 1, 2 e 3 completas

### ML-4A — Testes Go (endpoints + path traversal)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/api_board_test.go`
- `internal/serve/api_file_test.go`
- `internal/serve/metrics_log_test.go`

**Critérios de aceite:**
- [ ] `go test ./internal/serve/...` verde
- [ ] `api_file_test.go` cobre path traversal (403)
- [ ] `metrics_log_test.go` cobre log vazio e log com dados

### ML-4B — Testes Node.js
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `npm/tests/serve_api.test.js` (novo)

**Critérios de aceite:**
- [ ] `node npm/tests/serve_api.test.js` verde
- [ ] Cobre `/api/board`, `/api/file` path traversal, `/api/metrics` sem log

### ML-4C — Testes Python
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `pypi/tests/test_serve_api.py` (novo)

**Critérios de aceite:**
- [ ] `pytest pypi/tests/test_serve_api.py` verde

### ML-4D — `--open` + graceful shutdown + help text (3 CLIs)
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `internal/serve/serve.go` (Go: SIGINT/SIGTERM + `--open` darwin)
- `internal/commands/serve.go` (Go: help text)
- `npm/src/commands/serve.js` (Node.js: `--open` + SIGINT)
- `pypi/trackfw/commands/serve.py` (Python: `--open` + SIGINT)

**Critérios de aceite:**
- [ ] `trackfw serve --open` abre browser no macOS (3 CLIs)
- [ ] `trackfw serve --no-open` não abre browser
- [ ] Ctrl+C encerra sem panic/traceback (3 CLIs)
- [ ] `trackfw serve --help` descreve flags e views

### ML-4E — README + VISION atualização
**Status:** ⬜ Pendente
**Arquivos afetados:**
- `README.md`
- `docs/visao-projeto/VISION.md`

**Critérios de aceite:**
- [ ] `trackfw serve` documentado com paridade nos 3 CLIs
- [ ] Views (Kanban, Chain, Métricas/Burndown) descritas

---

## Comandos de validação final

```bash
# Go
make build && go test ./...
./bin/trackfw serve --no-open &
curl localhost:8080/api/board | jq .
curl localhost:8080/api/chain | jq .
curl localhost:8080/api/metrics | jq .
curl "localhost:8080/api/file?path=../../../etc/passwd"  # deve retornar 403
kill %1

# Node.js
node npm/src/index.js serve --no-open &
curl localhost:8080/api/board | jq .
kill %1

# Python
python -m trackfw serve --no-open &
curl localhost:8080/api/board | jq .
kill %1
```
