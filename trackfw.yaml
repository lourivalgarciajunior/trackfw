# trackfw — configuração de governança deste repositório
#
# Este repo faz dogfooding do próprio trackfw. Os artefatos ficam namespaced por
# agente (by_agent) e o diretório de requisitos usa o nome em português, herdado
# do histórico do projeto. Ver REQ-2026-08-16-consolidar-arvores-governanca.

adr_dirs:
  - docs/adr

req_dir: docs/requisições
roadmap_dir: docs/roadmaps

roadmap_namespacing: by_agent
agents:
  - apolo
  - artemis
  - claude

governance_mode: strict
wip_limit: 1

# Nenhuma regra rebaixada — todas no default.
#
# `req_has_adr` ficou em `warning` entre 2026-08-16 e 2026-08-17, enquanto as 5
# REQs legadas não tinham ADR nenhuma. Não era discordância da regra, era falta
# de saída. Com as ADRs retroativas escritas em REQ-2026-08-16-adrs-retroativas,
# voltou ao default `error`.
